import { InputType, Field } from 'type-graphql';

import { <%= name %> } from '@/entities/<%= name %>';

@InputType()
export class Update<%= name %>Input implements Partial<<%= name %>> {
  @Field({ nullable: true })
  name?: string;
}
